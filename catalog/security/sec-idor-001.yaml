id: sec-idor-001
bundle: security
category: access-control
pool: public
evidence_model: oracle
penalty: 40   # horizontal IDOR — broken access control is the #1 real-world slop class
applicability:
  requires: [has_auth_entrypoint]
probe:
  predicate: idor_horizontal
severity_ref: access-control   # shared block in catalog/_severity_classes.yaml (idor_horizontal sets cross_user_read)
