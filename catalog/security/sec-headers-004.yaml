id: sec-headers-004
bundle: security
category: security-headers
pool: public
evidence_model: provable
penalty: 5
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  target: /   # clickjacking defense is global -> check once on the homepage
slop_if:
  - response_missing_clickjacking_defense   # neither X-Frame-Options nor CSP frame-ancestors present
