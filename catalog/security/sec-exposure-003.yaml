id: sec-exposure-003
bundle: security
category: exposure
variant_group_id: exposed-git
pool: public
evidence_model: provable
penalty: 30
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  target: /.git/HEAD
slop_if:
  - response_is_git_head
