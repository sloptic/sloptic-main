id: perf-loadtime-001
bundle: performance
category: load-time
pool: public
evidence_model: provable
penalty: 34   # load past the ~5s abandonment ceiling. 53% of mobile visits bounce >3s
              # (Google 2016); 79% won't return (Akamai). Flagship high-freq churn -> qa/perf top.
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  predicate: perf_load_time
  target: /
