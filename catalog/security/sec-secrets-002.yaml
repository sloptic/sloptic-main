id: sec-secrets-002
bundle: security
category: secrets-exposure
variant_group_id: secrets-exposure   # same "this app leaks a secret" finding as sec-secrets-001 -> one finding
pool: public
evidence_model: provable
penalty: 35   # a SERVER secret key (Stripe sk_ / OpenAI / AWS secret / private key) shipped in the client JS
              # bundle = account/DB takeover — the #1 real SPA leak; public anon/publishable keys are excluded
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  predicate: bundle_leaks_secret
