id: sec-session-002
bundle: security
category: session
pool: public
evidence_model: oracle
penalty: 15   # a session cookie without SameSite is sendable cross-site (CSRF surface)
applicability:
  requires: [has_auth_entrypoint]
probe:
  predicate: session_cookie_missing_flag
  flag: samesite
